
vscode.deb
Prompts/
logistics/assets/Master copies/
qc-slop-audit.md
status-write-site-inventory.md

# OAuth client secret downloads — never commit, values are stored as Worker secrets
client_secret_*.json

# Security remediation (2026-07-31): DB_Migrations/ has contained plaintext employee/admin
# credentials (seed-employees.sql, auth.sql) and employee_roster.md contained real employee
# names/roles. Full git history was purged of both paths (git filter-repo) and force-pushed.
# Migration .sql files still live locally and are still authored/run manually per AGENTS.md —
# they are just no longer committed to version control. Never remove these two lines without
# auditing new migration files for secrets/PII first.
DB_Migrations/
employee_roster.md

# Source spreadsheet for the P323/P324 block-nesting baseline — kept local only. The parsed data
# it contains (item codes/dims/qty) is committed in cutting-pilot/src/lib/po1Fixture.ts instead.
xPanda_PO1_Nesting_Map.xlsx

# Source spreadsheet for the P411 "locked customer order" ground-truth fixture — kept local only,
# same policy as above (contains pricing). The parsed data (item codes/dims/qty) is committed in
# cutting-pilot/src/lib/lockedOrderFixture.ts instead.
Foam Purchase Orders (2).xlsx

# Labelife source label definition for the P421 DiversiTech label rework — kept local only, same
# policy as above. The parsed geometry (element x/y/w/h, font sizes, bold weights) is committed in
# jobs/diversitech-labels.js's DIVERSITECH_LAYOUT constant instead.
H1840.aml
